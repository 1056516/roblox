// Your game logic and functionality
// Example: Initialize game, handle user input, etc.
// Replace this with your actual game code
console.log("Now.gg game is loading...");

// Example event listener (you can add more)
document.addEventListener("keydown", (event) => {
    if (event.key === "Space") {
        // Handle spacebar press
        console.log("Spacebar pressed!");
    }
});
